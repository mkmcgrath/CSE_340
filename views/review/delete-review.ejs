<h1><%= title %></h1>

<div class="delete-confirmation">
  <p>Are you sure you want to delete this review?</p>

  <div class="review-preview">
    <p><strong>Rating:</strong>
      <% for(let i = 1; i <= 5; i++) { %>
        <% if(i <= locals.review_rating) { %>
          &#9733;
        <% } else { %>
          &#9734;
        <% } %>
      <% } %>
    </p>
    <p><strong>Review:</strong></p>
    <p><%= locals.review_text || '' %></p>
  </div>

  <form action="/review/delete" method="post">
    <input type="hidden" name="review_id" value="<%= locals.review_id || '' %>">
    <button type="submit" class="btn-delete">Yes, Delete Review</button>
    <a href="/account/" class="btn-cancel">No, Keep Review</a>
  </form>
</div>
